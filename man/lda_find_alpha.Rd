% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lda_find_alpha.R
\name{lda_find_alpha}
\alias{lda_find_alpha}
\title{Find Optimal Alpha Parameter for LDA}
\usage{
lda_find_alpha(
  dtm,
  candidate_alpha = c(10, 5, 2, 1),
  candidate_k = c(2, seq(5, 100, by = 5), 125, 150, 175, 200),
  folds = 5,
  ncores = NULL,
  seed = 123
)
}
\arguments{
\item{dtm}{A DocumentTermMatrix from the topicmodels package}

\item{candidate_alpha}{Numeric vector of alpha divisors to test (default: c(10, 5, 2, 1))}

\item{candidate_k}{Numeric vector of topic numbers to test (default: c(2, seq(5, 100, by=5), 125, 150, 175, 200))}

\item{folds}{Integer number of cross-validation folds (default: 5)}

\item{ncores}{Integer number of cores to use. If NULL, uses detectCores() - 2 (default: NULL)}

\item{seed}{Integer seed for reproducibility (default: 123)}
}
\value{
A data frame with columns: k, alpha, fold, perplexity, kalpha, newalpha, newalpha2
}
\description{
Performs k-fold cross-validation to identify optimal alpha values across
different topic numbers (k). Uses parallel processing for efficiency.
}
